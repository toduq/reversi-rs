<?xml version="1.0" standalone="no"?><!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd"><svg version="1.1" width="1200" height="566" onload="init(evt)" viewBox="0 0 1200 566" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:fg="http://github.com/jonhoo/inferno"><!--Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples.--><!--NOTES: --><defs><linearGradient id="background" y1="0" y2="1" x1="0" x2="0"><stop stop-color="#eeeeee" offset="5%"/><stop stop-color="#eeeeb0" offset="95%"/></linearGradient></defs><style type="text/css">
text { font-family:"Verdana"; font-size:12px; fill:rgb(0,0,0); }
#title { text-anchor:middle; font-size:17px; }
#search { opacity:0.1; cursor:pointer; }
#search:hover, #search.show { opacity:1; }
#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
#unzoom { cursor:pointer; }
#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
.hide { display:none; }
.parent { opacity:0.5; }
</style><script type="text/ecmascript"><![CDATA[
        var nametype = 'Function:';
        var fontsize = 12;
        var fontwidth = 0.59;
        var xpad = 10;
        var inverted = false;
        var searchcolor = 'rgb(230,0,230)';
        var fluiddrawing = true;
        var truncate_text_right = false;
    ]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching, frames;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    frames = document.getElementById("frames");
    total_samples = parseInt(frames.attributes.total_samples.value);
    searching = 0;

    // Use GET parameters to restore a flamegraph's state.
    var restore_state = function() {
        var params = get_params();
        if (params.x && params.y)
            zoom(find_group(document.querySelector('[*|x="' + params.x + '"][y="' + params.y + '"]')));
        if (params.s)
            search(params.s);
    };

    if (fluiddrawing) {
        // Make width dynamic so the SVG fits its parent's width.
        svg.removeAttribute("width");
        // Edge requires us to have a viewBox that gets updated with size changes.
        var isEdge = /Edge\/\d./i.test(navigator.userAgent);
        if (!isEdge) {
          svg.removeAttribute("viewBox");
        }
        var update_for_width_change = function() {
            if (isEdge) {
                svg.attributes.viewBox.value = "0 0 " + svg.width.baseVal.value + " " + svg.height.baseVal.value;
            }

            // Keep consistent padding on left and right of frames container.
            frames.attributes.width.value = svg.width.baseVal.value - xpad * 2;

            // Text truncation needs to be adjusted for the current width.
            var el = frames.children;
            for(var i = 0; i < el.length; i++) {
                update_text(el[i]);
            }

            // Keep search elements at a fixed distance from right edge.
            var svgWidth = svg.width.baseVal.value;
            searchbtn.attributes.x.value = svgWidth - xpad - 100;
            matchedtxt.attributes.x.value = svgWidth - xpad - 100;
        };
        window.addEventListener('resize', function() {
            update_for_width_change();
        });
        // This needs to be done asynchronously for Safari to work.
        setTimeout(function() {
            unzoom();
            update_for_width_change();
            restore_state();
        }, 0);
    } else {
        restore_state();
    }
}
// event listeners
window.addEventListener("click", function(e) {
    var target = find_group(e.target);
    if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);

        // set parameters for zoom state
        var el = target.querySelector("rect");
        if (el && el.attributes && el.attributes.y && el.attributes["fg:x"]) {
            var params = get_params()
            params.x = el.attributes["fg:x"].value;
            params.y = el.attributes.y.value;
            history.replaceState(null, null, parse_params(params));
        }
    }
    else if (e.target.id == "unzoom") {
        unzoom();

        // remove zoom state
        var params = get_params();
        if (params.x) delete params.x;
        if (params.y) delete params.y;
        history.replaceState(null, null, parse_params(params));
    }
    else if (e.target.id == "search") search_prompt();
}, false)
// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
    var target = find_group(e.target);
    if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function get_params() {
    var params = {};
    var paramsarr = window.location.search.substr(1).split('&');
    for (var i = 0; i < paramsarr.length; ++i) {
        var tmp = paramsarr[i].split("=");
        if (!tmp[0] || !tmp[1]) continue;
        params[tmp[0]]  = decodeURIComponent(tmp[1]);
    }
    return params;
}
function parse_params(params) {
    var uri = "?";
    for (var key in params) {
        uri += key + '=' + encodeURIComponent(params[key]) + '&';
    }
    if (uri.slice(-1) == "&")
        uri = uri.substring(0, uri.length - 1);
    if (uri == '?')
        uri = window.location.href.split('?')[0];
    return uri;
}
function find_child(node, selector) {
    var children = node.querySelectorAll(selector);
    if (children.length) return children[0];
    return;
}
function find_group(node) {
    var parent = node.parentElement;
    if (!parent) return;
    if (parent.id == "frames") return node;
    return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["fg:orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("fg:orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["fg:orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["fg:orig_" + attr].value;
    e.removeAttribute("fg:orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) * frames.attributes.width.value / 100 - 3;
    var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
    t.attributes.x.value = format_percent((parseFloat(r.attributes.x.value) + (100 * 3 / frames.attributes.width.value)));
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getComputedTextLength() < w)
        return;
    if (truncate_text_right) {
        // Truncate the right side of the text.
        for (var x = txt.length - 2; x > 0; x--) {
            if (t.getSubStringLength(0, x + 2) <= w) {
                t.textContent = txt.substring(0, x) + "..";
                return;
            }
        }
    } else {
        // Truncate the left side of the text.
        for (var x = 2; x < txt.length; x++) {
            if (t.getSubStringLength(x - 2, txt.length) <= w) {
                t.textContent = ".." + txt.substring(x, txt.length);
                return;
            }
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * parseInt(e.attributes["fg:x"].value) / total_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / total_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, zoomed_width_samples) {
    if (e.tagName == "text") {
        var parent_x = parseFloat(find_child(e.parentNode, "rect[x]").attributes.x.value);
        e.attributes.x.value = format_percent(parent_x + (100 * 3 / frames.attributes.width.value));
    } else if (e.tagName == "rect") {
        e.attributes.x.value = format_percent(100 * (parseInt(e.attributes["fg:x"].value) - x) / zoomed_width_samples);
        e.attributes.width.value = format_percent(100 * parseInt(e.attributes["fg:w"].value) / zoomed_width_samples);
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x, zoomed_width_samples);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            e.attributes.x.value = "0.0%";
        }
        if (e.attributes.width != undefined) {
            e.attributes.width.value = "100.0%";
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseInt(attr["fg:w"].value);
    var xmin = parseInt(attr["fg:x"].value);
    var xmax = xmin + width;
    var ymin = parseFloat(attr.y.value);
    unzoombtn.classList.remove("hide");
    var el = frames.children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseInt(a["fg:x"].value);
        var ew = parseInt(a["fg:w"].value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, width);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = frames.children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
    var params = get_params();
    delete params.s;
    history.replaceState(null, null, parse_params(params));
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = frames.children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        // Skip over frames which are either not visible, or below the zoomed-to frame
        if (e.classList.contains("hide") || e.classList.contains("parent")) {
            continue;
        }
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseInt(rect.attributes["fg:w"].value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseInt(rect.attributes["fg:x"].value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    var params = get_params();
    params.s = term;
    history.replaceState(null, null, parse_params(params));

    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    for (var k in keys) {
        var x = parseInt(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
function format_percent(n) {
    return n.toFixed(4) + "%";
}
]]></script><rect x="0" y="0" width="100%" height="566" fill="url(#background)"/><text id="title" x="50.0000%" y="24.00">Flame Graph</text><text id="details" x="10" y="549.00"> </text><text id="unzoom" class="hide" x="10" y="24.00">Reset Zoom</text><text id="search" x="1090" y="24.00">Search</text><text id="matched" x="1090" y="549.00"> </text><svg id="frames" x="10" width="1180" total_samples="143941"><g><title>reversi-rs`reversi_rs::mobility::put (23 samples, 0.02%)</title><rect x="0.0146%" y="245" width="0.0160%" height="15" fill="rgb(227,0,7)" fg:x="21" fg:w="23"/><text x="0.2646%" y="255.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (65 samples, 0.05%)</title><rect x="0.0535%" y="229" width="0.0452%" height="15" fill="rgb(217,0,24)" fg:x="77" fg:w="65"/><text x="0.3035%" y="239.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (30 samples, 0.02%)</title><rect x="0.1341%" y="213" width="0.0208%" height="15" fill="rgb(221,193,54)" fg:x="193" fg:w="30"/><text x="0.3841%" y="223.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (213 samples, 0.15%)</title><rect x="0.1549%" y="213" width="0.1480%" height="15" fill="rgb(248,212,6)" fg:x="223" fg:w="213"/><text x="0.4049%" y="223.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (102 samples, 0.07%)</title><rect x="0.3995%" y="197" width="0.0709%" height="15" fill="rgb(208,68,35)" fg:x="575" fg:w="102"/><text x="0.6495%" y="207.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (409 samples, 0.28%)</title><rect x="0.4703%" y="197" width="0.2841%" height="15" fill="rgb(232,128,0)" fg:x="677" fg:w="409"/><text x="0.7203%" y="207.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (234 samples, 0.16%)</title><rect x="1.0449%" y="181" width="0.1626%" height="15" fill="rgb(207,160,47)" fg:x="1504" fg:w="234"/><text x="1.2949%" y="191.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (1,328 samples, 0.92%)</title><rect x="1.2074%" y="181" width="0.9226%" height="15" fill="rgb(228,23,34)" fg:x="1738" fg:w="1328"/><text x="1.4574%" y="191.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (715 samples, 0.50%)</title><rect x="2.7150%" y="165" width="0.4967%" height="15" fill="rgb(218,30,26)" fg:x="3908" fg:w="715"/><text x="2.9650%" y="175.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (2,220 samples, 1.54%)</title><rect x="3.2117%" y="165" width="1.5423%" height="15" fill="rgb(220,122,19)" fg:x="4623" fg:w="2220"/><text x="3.4617%" y="175.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (1,340 samples, 0.93%)</title><rect x="6.0108%" y="149" width="0.9309%" height="15" fill="rgb(250,228,42)" fg:x="8652" fg:w="1340"/><text x="6.2608%" y="159.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (5,368 samples, 3.73%)</title><rect x="6.9417%" y="149" width="3.7293%" height="15" fill="rgb(240,193,28)" fg:x="9992" fg:w="5368"/><text x="7.1917%" y="159.50">reve..</text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (3,275 samples, 2.28%)</title><rect x="13.0269%" y="133" width="2.2752%" height="15" fill="rgb(216,20,37)" fg:x="18751" fg:w="3275"/><text x="13.2769%" y="143.50">r..</text></g><g><title>reversi-rs`reversi_rs::mobility::put (7,610 samples, 5.29%)</title><rect x="15.3021%" y="133" width="5.2869%" height="15" fill="rgb(206,188,39)" fg:x="22026" fg:w="7610"/><text x="15.5521%" y="143.50">revers..</text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (5,382 samples, 3.74%)</title><rect x="24.4559%" y="117" width="3.7390%" height="15" fill="rgb(217,207,13)" fg:x="35202" fg:w="5382"/><text x="24.7059%" y="127.50">reve..</text></g><g><title>reversi-rs`reversi_rs::mobility::put (13,671 samples, 9.50%)</title><rect x="28.1949%" y="117" width="9.4976%" height="15" fill="rgb(231,73,38)" fg:x="40584" fg:w="13671"/><text x="28.4449%" y="127.50">reversi-rs`rev..</text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (10,077 samples, 7.00%)</title><rect x="43.6450%" y="101" width="7.0008%" height="15" fill="rgb(225,20,46)" fg:x="62823" fg:w="10077"/><text x="43.8950%" y="111.50">reversi-r..</text></g><g><title>reversi-rs`reversi_rs::mobility::put (13,612 samples, 9.46%)</title><rect x="50.6458%" y="101" width="9.4567%" height="15" fill="rgb(210,31,41)" fg:x="72900" fg:w="13612"/><text x="50.8958%" y="111.50">reversi-rs`rev..</text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (12,313 samples, 8.55%)</title><rect x="66.6905%" y="85" width="8.5542%" height="15" fill="rgb(221,200,47)" fg:x="95995" fg:w="12313"/><text x="66.9405%" y="95.50">reversi-rs`r..</text></g><g><title>reversi-rs`reversi_rs::mobility::put (14,111 samples, 9.80%)</title><rect x="75.2447%" y="85" width="9.8033%" height="15" fill="rgb(226,26,5)" fg:x="108308" fg:w="14111"/><text x="75.4947%" y="95.50">reversi-rs`rev..</text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (3,878 samples, 2.69%)</title><rect x="91.4243%" y="69" width="2.6942%" height="15" fill="rgb(249,33,26)" fg:x="131597" fg:w="3878"/><text x="91.6743%" y="79.50">re..</text></g><g><title>reversi-rs`reversi_rs::mobility::put (3,545 samples, 2.46%)</title><rect x="94.1184%" y="69" width="2.4628%" height="15" fill="rgb(235,183,28)" fg:x="135475" fg:w="3545"/><text x="94.3684%" y="79.50">re..</text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (632 samples, 0.44%)</title><rect x="98.5230%" y="53" width="0.4391%" height="15" fill="rgb(221,5,38)" fg:x="141815" fg:w="632"/><text x="98.7730%" y="63.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (821 samples, 0.57%)</title><rect x="98.9621%" y="53" width="0.5704%" height="15" fill="rgb(247,18,42)" fg:x="142447" fg:w="821"/><text x="99.2121%" y="63.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::get_mobility (43 samples, 0.03%)</title><rect x="99.9027%" y="37" width="0.0299%" height="15" fill="rgb(241,131,45)" fg:x="143801" fg:w="43"/><text x="100.1527%" y="47.50"></text></g><g><title>reversi-rs`reversi_rs::mobility::put (51 samples, 0.04%)</title><rect x="99.9326%" y="37" width="0.0354%" height="15" fill="rgb(249,31,29)" fg:x="143844" fg:w="51"/><text x="100.1826%" y="47.50"></text></g><g><title>all (143,941 samples, 100%)</title><rect x="0.0000%" y="517" width="100.0000%" height="15" fill="rgb(225,111,53)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="527.50"></text></g><g><title>0x6 (143,941 samples, 100.00%)</title><rect x="0.0000%" y="501" width="100.0000%" height="15" fill="rgb(238,160,17)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="511.50">0x6</text></g><g><title>reversi-rs`start (143,941 samples, 100.00%)</title><rect x="0.0000%" y="485" width="100.0000%" height="15" fill="rgb(214,148,48)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="495.50">reversi-rs`start</text></g><g><title>reversi-rs`main (143,941 samples, 100.00%)</title><rect x="0.0000%" y="469" width="100.0000%" height="15" fill="rgb(232,36,49)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="479.50">reversi-rs`main</text></g><g><title>reversi-rs`std::rt::lang_start_internal (143,941 samples, 100.00%)</title><rect x="0.0000%" y="453" width="100.0000%" height="15" fill="rgb(209,103,24)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="463.50">reversi-rs`std::rt::lang_start_internal</text></g><g><title>reversi-rs`std::rt::lang_start::_{{closure}} (143,941 samples, 100.00%)</title><rect x="0.0000%" y="437" width="100.0000%" height="15" fill="rgb(229,88,8)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="447.50">reversi-rs`std::rt::lang_start::_{{closure}}</text></g><g><title>reversi-rs`std::sys_common::backtrace::__rust_begin_short_backtrace (143,941 samples, 100.00%)</title><rect x="0.0000%" y="421" width="100.0000%" height="15" fill="rgb(213,181,19)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="431.50">reversi-rs`std::sys_common::backtrace::__rust_begin_short_backtrace</text></g><g><title>reversi-rs`reversi_rs::main (143,941 samples, 100.00%)</title><rect x="0.0000%" y="405" width="100.0000%" height="15" fill="rgb(254,191,54)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="415.50">reversi-rs`reversi_rs::main</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,941 samples, 100.00%)</title><rect x="0.0000%" y="389" width="100.0000%" height="15" fill="rgb(241,83,37)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="399.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,941 samples, 100.00%)</title><rect x="0.0000%" y="373" width="100.0000%" height="15" fill="rgb(233,36,39)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="383.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,941 samples, 100.00%)</title><rect x="0.0000%" y="357" width="100.0000%" height="15" fill="rgb(226,3,54)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="367.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,941 samples, 100.00%)</title><rect x="0.0000%" y="341" width="100.0000%" height="15" fill="rgb(245,192,40)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="351.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,941 samples, 100.00%)</title><rect x="0.0000%" y="325" width="100.0000%" height="15" fill="rgb(238,167,29)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="335.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,941 samples, 100.00%)</title><rect x="0.0000%" y="309" width="100.0000%" height="15" fill="rgb(232,182,51)" fg:x="0" fg:w="143941"/><text x="0.2500%" y="319.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,938 samples, 100.00%)</title><rect x="0.0021%" y="293" width="99.9979%" height="15" fill="rgb(231,60,39)" fg:x="3" fg:w="143938"/><text x="0.2521%" y="303.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,936 samples, 100.00%)</title><rect x="0.0035%" y="277" width="99.9965%" height="15" fill="rgb(208,69,12)" fg:x="5" fg:w="143936"/><text x="0.2535%" y="287.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,929 samples, 99.99%)</title><rect x="0.0083%" y="261" width="99.9917%" height="15" fill="rgb(235,93,37)" fg:x="12" fg:w="143929"/><text x="0.2583%" y="271.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,897 samples, 99.97%)</title><rect x="0.0306%" y="245" width="99.9694%" height="15" fill="rgb(213,116,39)" fg:x="44" fg:w="143897"/><text x="0.2806%" y="255.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,799 samples, 99.90%)</title><rect x="0.0987%" y="229" width="99.9013%" height="15" fill="rgb(222,207,29)" fg:x="142" fg:w="143799"/><text x="0.3487%" y="239.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (143,505 samples, 99.70%)</title><rect x="0.3029%" y="213" width="99.6971%" height="15" fill="rgb(206,96,30)" fg:x="436" fg:w="143505"/><text x="0.5529%" y="223.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (142,855 samples, 99.25%)</title><rect x="0.7545%" y="197" width="99.2455%" height="15" fill="rgb(218,138,4)" fg:x="1086" fg:w="142855"/><text x="1.0045%" y="207.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (140,875 samples, 97.87%)</title><rect x="2.1300%" y="181" width="97.8700%" height="15" fill="rgb(250,191,14)" fg:x="3066" fg:w="140875"/><text x="2.3800%" y="191.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (137,098 samples, 95.25%)</title><rect x="4.7540%" y="165" width="95.2460%" height="15" fill="rgb(239,60,40)" fg:x="6843" fg:w="137098"/><text x="5.0040%" y="175.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (128,581 samples, 89.33%)</title><rect x="10.6710%" y="149" width="89.3290%" height="15" fill="rgb(206,27,48)" fg:x="15360" fg:w="128581"/><text x="10.9210%" y="159.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (114,305 samples, 79.41%)</title><rect x="20.5890%" y="133" width="79.4110%" height="15" fill="rgb(225,35,8)" fg:x="29636" fg:w="114305"/><text x="20.8390%" y="143.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (89,686 samples, 62.31%)</title><rect x="37.6925%" y="117" width="62.3075%" height="15" fill="rgb(250,213,24)" fg:x="54255" fg:w="89686"/><text x="37.9425%" y="127.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (57,429 samples, 39.90%)</title><rect x="60.1024%" y="101" width="39.8976%" height="15" fill="rgb(247,123,22)" fg:x="86512" fg:w="57429"/><text x="60.3524%" y="111.50">reversi-rs`reversi_rs::search::rec_search</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (21,522 samples, 14.95%)</title><rect x="85.0480%" y="85" width="14.9520%" height="15" fill="rgb(231,138,38)" fg:x="122419" fg:w="21522"/><text x="85.2980%" y="95.50">reversi-rs`reversi_rs::..</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (4,921 samples, 3.42%)</title><rect x="96.5812%" y="69" width="3.4188%" height="15" fill="rgb(231,145,46)" fg:x="139020" fg:w="4921"/><text x="96.8312%" y="79.50">rev..</text></g><g><title>reversi-rs`reversi_rs::search::rec_search (673 samples, 0.47%)</title><rect x="99.5324%" y="53" width="0.4676%" height="15" fill="rgb(251,118,11)" fg:x="143268" fg:w="673"/><text x="99.7824%" y="63.50"></text></g><g><title>reversi-rs`reversi_rs::search::rec_search (46 samples, 0.03%)</title><rect x="99.9680%" y="37" width="0.0320%" height="15" fill="rgb(217,147,25)" fg:x="143895" fg:w="46"/><text x="100.2180%" y="47.50"></text></g></svg></svg>